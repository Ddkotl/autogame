{
  "type": "commonjs",
  "scripts": {
    "prettier": "prettier -w .",
    "build": "rm -rf dist && tsc",
    "start": "node dist/index.js",
    "go": "npm run build && npm run start"
  },
  "dependencies": {
    "playwright": "^1.54.2"
  },
  "devDependencies": {
    "@types/node": "^24.2.0",
    "prettier": "^3.6.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}
