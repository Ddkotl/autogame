name: Bue Virus Script

on:
  workflow_dispatch:
  schedule:
    - cron: "30 0 * * *"

jobs:
  bue-virus:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: npm ci
      - run: npm run build
      - run: npm run bue_virus
